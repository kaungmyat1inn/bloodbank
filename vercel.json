{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b 3.47.2 _flutter_sdk --depth 1 && export PATH=\"$PATH:$(pwd)/_flutter_sdk/bin\" && flutter config --enable-web --no-analytics && flutter create --platforms=web . && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo 'skip default install, handled in buildCommand'",
  "framework": null
}
